<Window x:Class="RxTickets.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RxTickets"
        mc:Ignorable="d"
        Title="RX Tickets" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=local:MainWindowVM, IsDesignTimeCreatable=True}">
    <Window.DataContext>
        <local:MainWindowVM />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <!--Tickets Dashboard-->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Grid Grid.Row="0" Background="LightGray">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ToggleButton IsChecked="{Binding TicketsDashboardVM.IncludeClosed}" Grid.Column="0" Content="Show Closed" Margin="5,0" VerticalAlignment="Center"/>
                <TextBox Text="{Binding TicketsDashboardVM.SearchQuery,UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" VerticalAlignment="Center" Margin="5,0" />
            </Grid>
            <ListBox Grid.Row="1" ItemsSource="{Binding TicketsDashboardVM.Tickets}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <StackPanel>
                                <TextBlock Text="Title:"/>
                                <TextBlock Text="{Binding Title}"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Author:"/>
                                <TextBlock Text="{Binding Author}"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Created On:"/>
                                <TextBlock Text="{Binding CreatedOn}"/>
                            </StackPanel>
                        </StackPanel>
                        
                        
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
        
        <!--Developer options lsit-->
        <!--<ListBox Grid.Column="1">
            <ListBoxItem>
                <DataTemplate>
                    <Button/>
                </DataTemplate>
            </ListBoxItem>
        </ListBox>-->
    </Grid>
</Window>
